[pytest]
markers =
    negative: the app can handle unwanted input and user behavior.
    basic: focused on core features.
    slow: will take longer to run.
    integration: external systems and calls.